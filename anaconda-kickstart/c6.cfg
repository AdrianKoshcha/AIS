#platform=x86, AMD64, or Intel EM64T
#version=DEVEL
# Firewall configuration
firewall --enabled --ssh
# Install OS instead of upgrade
install
# Use network installation
url --url="http://mirror.rit.edu/centos/6/os/x86_64"
# Root password
rootpw --iscrypted $1$cuCGJUAj$mBungq/ML3U5hUAltZcL6/
# System authorization information
auth  --useshadow  --passalgo=sha512
# Use text mode install
text
# System keyboard
keyboard us
# System language
lang en_US
# SELinux configuration
selinux --enforcing
# Do not configure the X Window System
skipx
# Installation logging level
logging --level=info
# Reboot after installation
reboot
# System timezone
timezone --isUtc America/New_York
user --groups=wheel --name=adrian --password=$1$cuCGJUAj$mBungq/ML3U5hUAltZcL6/ --iscrypted --gecos="Adrian Veektorija Koshkayatchkov"
# Network information
network  --bootproto=dhcp --device=eth0 --onboot=on
# System bootloader configuration
bootloader --location=mbr
# Clear the Master Boot Record
zerombr
# Partition clearing information
clearpart --all --initlabel 
# Disk partitioning information
part swap --fstype="swap" --recommended
part / --asprimary --fstype="ext4" --grow --size=1

%packages
@base
@system-admin-tools

%end
